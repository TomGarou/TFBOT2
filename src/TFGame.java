import org.w3c.dom.Document;

public class TFGame 
{
	public TFGame(Document doc)
	{
	
	//This class will call the parser and generate the Game Data used in the main class.
	TransformationFactory tfParser = new TransformationFactory(doc);
	
	}		

}
